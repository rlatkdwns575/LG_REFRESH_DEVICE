class HairRefresherDevice {
  const HairRefresherDevice({
    required this.id,
    required this.name,
    required this.isConnected,
  });

  final String id;
  final String name;
  final bool isConnected;
}
